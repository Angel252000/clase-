# ═══════════════════════════════════════════════════════
# CREAMY Client — Variables de entorno
# ═══════════════════════════════════════════════════════
# 1. Copia este archivo: cp .env.example .env
# 2. Rellena los valores reales
# 3. NUNCA subas el .env a Git
# ═══════════════════════════════════════════════════════

# ── Stripe clave PÚBLICA (se expone al navegador — es seguro) ──
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_AQUI_VA_TU_CLAVE_PUBLICA

# ── URL del backend ─────────────────────────────────────
VITE_API_URL=http://localhost:4000
